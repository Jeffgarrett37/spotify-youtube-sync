# Runtime dependencies. Kept intentionally small; all pure-Python so the
# image builds on amd64 / aarch64 / armv7 (musl) without a compiler.
requests==2.32.3
tenacity==9.0.0
# IANA tz database - needed for correct US-Pacific quota-day math on hosts
# without a system zoneinfo (e.g. Windows, minimal containers).
tzdata==2024.2
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
